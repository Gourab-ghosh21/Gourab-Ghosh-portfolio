# ==========================================
# Gourab Ghosh Portfolio — Frontend Environment
# ==========================================

# Optional: URL of deployed backend service (e.g., https://your-backend.vercel.app)
# When left empty, frontend uses relative /api/contact (ideal for same-origin proxy or Vercel rewrites)
VITE_API_URL=
